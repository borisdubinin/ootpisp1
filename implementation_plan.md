# Implementation Plan: Графический редактор фигур (C++ / SFML)

---

## Общее видение

Приложение — это минималистичный **векторный графический редактор** в духе Figma/Inkscape, но упрощённый. Пользователь видит бесконечный (или большой) холст, на котором расположены фигуры. Интерфейс разделён на зоны: панель инструментов, холст, панель свойств. Всё управление через мышь + клавиатуру.

---

## Фазы разработки

---

### Фаза 0 — Настройка проекта (1–2 дня)

**Цель:** рабочее окружение, компилируется пустое окно.

- Установить SFML (или альтернативу: SDL2 + Cairo, Qt)
- Настроить CMakeLists.txt / Makefile
- Создать структуру папок проекта:
  ```
  /src
    /core         — логика (фигуры, сцена)
    /ui           — интерфейс (панели, кнопки, поля ввода)
    /renderer     — отрисовка
    /input        — обработка событий
  /assets         — иконки, шрифты
  main.cpp
  ```
- Настроить полноэкранное окно (без taskbar):
  ```cpp
  sf::RenderWindow window(
    sf::VideoMode::getDesktopMode(),
    "GraphEditor",
    sf::Style::Default  // или sf::Style::Fullscreen
  );
  ```
- Подключить шрифт (например, Roboto / Inter) для UI-текста

---

### Фаза 1 — Архитектура классов (2–3 дня)

**Цель:** чистая иерархия, не зависящая от рендеринга.

#### 1.1 Базовый класс `Figure`

```cpp
class Figure {
public:
    // Точка привязки (абсолютные координаты на холсте)
    sf::Vector2f anchor;

    // Цвет заливки
    sf::Color fillColor;

    // Виртуальные границы (bounding box)
    sf::FloatRect getBoundingBox() const;

    // Вершины (относительно anchor)
    virtual std::vector<sf::Vector2f> getVertices() const = 0;

    // Отрисовка
    virtual void draw(sf::RenderTarget& target) = 0;

    // Проверка попадания курсора
    virtual bool contains(sf::Vector2f point) const = 0;

    // Перемещение
    void move(sf::Vector2f delta);

protected:
    struct Edge {
        float width;       // ширина линии
        sf::Color color;   // цвет линии
    };
    std::vector<Edge> edges;
};
```

#### 1.2 Дочерние классы

| Класс | Особенности |
|---|---|
| `Rectangle` | 4 вершины, 4 грани |
| `Triangle` | 3 вершины, 3 грани |
| `Hexagon` | 6 вершин, 6 граней |
| `Rhombus` | 4 вершины, задаются диагонали |
| `Trapezoid` | 4 вершины, две параллельные стороны |

Каждый класс реализует `getVertices()`, `draw()`, `contains()`.

#### 1.3 Класс `Scene`

```cpp
class Scene {
    std::vector<std::unique_ptr<Figure>> figures;
    Figure* selectedFigure = nullptr;
public:
    void addFigure(std::unique_ptr<Figure> fig);
    void removeFigure(Figure* fig);
    Figure* hitTest(sf::Vector2f point);
    void drawAll(sf::RenderTarget& target);
};
```

---

### Фаза 2 — Рендеринг фигур (3–4 дня)

**Цель:** корректная отрисовка фигур с заливкой и цветными гранями.

#### 2.1 Заливка

Использовать `sf::ConvexShape` для заливки внутренней области.

#### 2.2 Рендеринг граней с учётом ширины

SFML рисует линии через `sf::RectangleShape` (развёрнутый вдоль ребра прямоугольник):

```
Для каждого ребра (A → B):
  1. Вычислить вектор направления и нормаль
  2. Построить прямоугольник шириной edge.width
  3. Залить цветом edge.color
```

#### 2.3 Корректная стыковка линий (ключевой нюанс)

Когда две грани встречаются в вершине и имеют **разную ширину / цвет**, нужно:

1. Вычислить биссектрису угла между двумя рёбрами
2. Обрезать оба прямоугольника по биссектрисе (miter join)
3. Заполнить треугольный «зазор» одним из цветов (по договорённости — цветом более широкой линии)

Алгоритм:
```
Для каждой вершины Vi:
  ребро1 = (V(i-1) → Vi),  ребро2 = (Vi → V(i+1))
  найти точку пересечения внешних границ двух рёбер
  → это точка miter
  если miter слишком далеко (острый угол) — обрезать (bevel)
```

#### 2.4 Виртуальные границы (bounding box)

Отрисовывать пунктирным прямоугольником вокруг выделенной фигуры:

```cpp
sf::RectangleShape bbox;
bbox.setFillColor(sf::Color::Transparent);
bbox.setOutlineColor(sf::Color(0, 120, 215)); // синий как в Windows
bbox.setOutlineThickness(1.f);
// + уголки-маркеры для визуализации
```

---

### Фаза 3 — Пользовательский интерфейс (4–5 дней)

**Цель:** профессиональный UI, удобный как в графических редакторах.

#### 3.1 Общий Layout

```
┌─────────────────────────────────────────────────────┐
│  [Title Bar]  GraphEditor          [_ □ ×]          │
├──────┬──────────────────────────────────┬────────────┤
│      │                                  │            │
│ Tool │           CANVAS                 │ Properties │
│ bar  │                                  │  Panel     │
│      │                                  │ (toggle)   │
│      │                                  │            │
└──────┴──────────────────────────────────┴────────────┘
```

#### 3.2 Toolbar (левая панель, ~48px шириной)

Иконки инструментов (реализовать через `sf::RectangleShape` + текст или спрайты):

| Иконка | Инструмент | Горячая клавиша |
|---|---|---|
| ↖ | Выделение / перемещение | `V` |
| □ | Прямоугольник | `R` |
| △ | Треугольник | `T` |
| ⬡ | Шестиугольник | `H` |
| ◇ | Ромб | `D` |
| ⌶ | Трапеция | `Z` |
| 🗑 | Удалить выбранную | `Delete` |

Активный инструмент подсвечивается (другой фон кнопки).

#### 3.3 Canvas (центральная область)

- Фон: светло-серый (`#F0F0F0`) или белый
- Опциональная сетка (toggle `G`)
- Курсор меняется в зависимости от инструмента (`sf::Cursor`)
- При создании фигуры: drag для задания размера (как в Figma)

#### 3.4 Properties Panel (правая панель, ~280px)

Появляется/скрывается по нажатию **`P`** или **`Tab`**, либо автоматически при выделении фигуры.

**Структура панели:**

```
┌─────────────────────────────┐
│  ФИГУРА: Прямоугольник      │
├─────────────────────────────┤
│  ТОЧКА ПРИВЯЗКИ             │
│  X: [_______]  Y: [_______] │
├─────────────────────────────┤
│  ВЕРШИНЫ (относительные)    │
│  V1  dx: [___]  dy: [___]   │
│  V2  dx: [___]  dy: [___]   │
│  V3  dx: [___]  dy: [___]   │
│  V4  dx: [___]  dy: [___]   │
├─────────────────────────────┤
│  ГРАНИ                      │
│  #1  W:[__]  C:[████████]   │
│  #2  W:[__]  C:[████████]   │
│  #3  W:[__]  C:[████████]   │
│  #4  W:[__]  C:[████████]   │
├─────────────────────────────┤
│  ЗАЛИВКА                    │
│  Цвет: [████████████████]   │
├─────────────────────────────┤
│  BOUNDING BOX               │
│  TL: (___,___)              │
│  BR: (___,___)              │
└─────────────────────────────┘
```

#### 3.5 UI-компоненты (реализовать самостоятельно или взять ImGui)

> **Рекомендация:** использовать библиотеку **Dear ImGui** с бэкендом SFML (`imgui-sfml`). Это даёт готовые поля ввода, слайдеры, color picker буквально из коробки.

Если без ImGui — реализовать вручную:

- `TextInput` — поле ввода числа (клик → фокус → ввод цифр → Enter/Tab для подтверждения)
- `ColorButton` — кнопка, клик открывает Color Picker (реализовать как HSV-квадрат + полоса)
- `Label` — статичный текст
- `Panel` — контейнер с фоном и скроллом

---

### Фаза 4 — Управление и интерактивность (2–3 дня)

**Цель:** мышь + клавиатура работают плавно и предсказуемо.

#### 4.1 Выделение фигуры

```
MouseButtonPressed (Left):
  fig = scene.hitTest(mousePos)
  if fig != null:
    selectedFigure = fig
    dragOffset = mousePos - fig.anchor
    state = DRAGGING
  else:
    selectedFigure = null
```

#### 4.2 Перетаскивание

```
MouseMoved (while DRAGGING):
  selectedFigure.anchor = mousePos - dragOffset
  // проверить bounding box — не выходить за границы окна

MouseButtonReleased:
  state = IDLE
```

#### 4.3 Создание новой фигуры

```
Активен инструмент (не Selection):
MouseButtonPressed → запомнить startPos
MouseMoved        → показать preview фигуры (полупрозрачно)
MouseButtonReleased → создать фигуру с anchor = центр выделенной области
```

#### 4.4 Горячие клавиши

| Клавиша | Действие |
|---|---|
| `V` | Инструмент выделения |
| `R/T/H/D/Z` | Инструменты фигур |
| `P` или `Tab` | Показать/скрыть панель свойств |
| `G` | Показать/скрыть сетку |
| `Delete` | Удалить выбранную фигуру |
| `Escape` | Снять выделение / отменить создание |
| `Ctrl+Z` | Undo (опционально) |

---

### Фаза 5 — Color Picker (1–2 дня)

**Цель:** удобный выбор цвета без сторонних зависимостей (если не используется ImGui).

Реализовать всплывающее окно Color Picker:

```
┌────────────────────────┐
│  [HSV квадрат          │
│   насыщ./яркость]      │
│                        │
│  [──── Hue полоса ────]│
│  [── Alpha полоса ────]│
│                        │
│  R:[___] G:[___] B:[___│
│  Hex: [________]       │
│  [Отмена]   [ОК]       │
└────────────────────────┘
```

Если используется **ImGui** — `ImGui::ColorPicker4()` решает задачу за одну строку.

---

### Фаза 6 — Полировка (1–2 дня)

- Плавный hover-эффект на кнопках toolbar
- Курсор `sf::Cursor::Hand` при наведении на фигуру
- Курсор `sf::Cursor::SizeAll` при перетаскивании
- Подсветка выделенной фигуры (outline / bounding box с маркерами углов)
- Статусная строка внизу: координаты мыши, название выбранной фигуры
- Обработка ресайза окна

---

## Рекомендуемый стек

| Компонент | Вариант |
|---|---|
| Графика | **SFML 2.6** |
| UI-виджеты | **Dear ImGui** + **imgui-sfml** (сильно упрощает панель свойств) |
| Сборка | **CMake 3.20+** |
| Компилятор | GCC 12+ / MSVC 2022 / Clang 15+ |

---

## Порядок подключения ImGui + SFML

```cmake
# CMakeLists.txt
find_package(SFML 2.6 COMPONENTS graphics window system REQUIRED)
add_subdirectory(extern/imgui)
add_subdirectory(extern/imgui-sfml)
target_link_libraries(GraphEditor PRIVATE sfml-graphics ImGui-SFML::ImGui-SFML)
```

```cpp
// main.cpp
#include <SFML/Graphics.hpp>
#include <imgui.h>
#include <imgui-SFML.h>

ImGui::SFML::Init(window);
// в цикле:
ImGui::SFML::Update(window, deltaClock.restart());
// ... ImGui::Begin("Properties") ...
ImGui::SFML::Render(window);
```

---

## Итоговый чеклист

### Архитектура
- [ ] Базовый класс `Figure` с точкой привязки и гранями
- [ ] 5 дочерних классов фигур
- [ ] Класс `Scene` для управления коллекцией фигур

### Рендеринг
- [ ] Заливка фигур
- [ ] Цветные грани с переменной шириной
- [ ] Корректная стыковка граней (miter/bevel join)
- [ ] Bounding box для выделенной фигуры

### Интерфейс
- [ ] Полноэкранное окно с заголовком
- [ ] Toolbar с инструментами
- [ ] Холст с фоном и опциональной сеткой
- [ ] Панель свойств (toggle по `P`/`Tab`)
- [ ] Все поля в панели редактируемы
- [ ] Color Picker для граней и заливки

### Интерактивность
- [ ] Выделение фигуры кликом
- [ ] Перетаскивание мышью
- [ ] Создание фигур drag-ом
- [ ] Горячие клавиши
- [ ] Смена курсора по контексту
